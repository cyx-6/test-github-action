name: test

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch or tag to publish (manual run)"
        required: true
  pull_request:
  push:

jobs:
  build_libs:
    strategy:
      fail-fast: false
      matrix:
        arch: ["x86_64"]
        python-version: ["cp39", "cp310", "cp311", "cp312", "cp313", "cp314"]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
        with:
          repository: cyx-6/tvm-ffi
          ref: torch-c-dlpack-windows
          fetch-depth: 0
          submodules: recursive
          path: tvm-ffi
      - name: build libs
        shell: cmd
        run: ./tvm-ffi/addons/torch_c_dlpack_ext/build_aot_wheels.bat ${{ matrix.arch }} ${{ matrix.python-version }}
